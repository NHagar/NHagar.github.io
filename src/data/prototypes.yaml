# src/data/prototypes.yaml
# Schema:
# - name: String (Name of the prototype/project)
#   id: String (Unique identifier, e.g., "ai-research-assistants-comparative")
#   description: String (Short description)
#   link: String (URL to the prototype or internal path if hosted within the same site)
#   image: String (Path to a thumbnail image, e.g., /images/prototype-name.png) - Optional
#   tags: Array of Strings (e.g., ["Data Visualization", "LLM", "Tool"]) - Optional

- name: "Liquify"
  id: "liquify"
  description: "Cross-provider LLM memory management tool."
  link: "https://github.com/NHagar/liquify-llm"
  tags: ["LLM", "Data Management"]

- name: "LLM data schema generator"
  id: "llm-data-schema-generator"
  description: "An early attempt to automatically generate schema for structured outputs with LLMs."
  link: "https://github.com/NHagar/schema-generation"
  tags: ["LLM", "Document Processing"]
